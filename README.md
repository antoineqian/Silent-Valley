# Silent-Valley

# Building
The project has been built with CMake on MacOs. Clone the repo with the `--recursive` flag to get the required git modules. 
The project also has a dependency on SFML (https://www.sfml-dev.org/tutorials/2.5/start-osx.php)
