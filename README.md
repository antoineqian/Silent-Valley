# Silent-Valley

# Installation notes
The project has been built with VScode on MacOs.
Clone the repo with the `--recursive` flag to get the required git modules. 
The project has a dependency on SFML (https://www.sfml-dev.org/tutorials/2.5/start-osx.php)
The project can be built with cmake, the relative paths to assets will need to be modified
